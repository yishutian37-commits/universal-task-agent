# UTA RAG 文档与检索质量实施记录

## 范围

- 新增 PDF、DOCX 文档解析。
- 统一桌面、CLI、HTTP API 和目录摄入的文件类型。
- 在向量召回候选上叠加轻量中英文关键词重排。
- 保留现有 SQLite、Chunk、Embedder 和 Generator 接口。

## 验收

- PDF 页面文本可合并摄入，扫描件空文本明确失败。
- DOCX 段落和表格内容可摄入。
- 桌面原生文件选择器可选择四类文档。
- 查询词与候选片段的中英文重合度参与最终排序。
- 打包应用包含解析依赖，完整测试和压缩包校验通过。

