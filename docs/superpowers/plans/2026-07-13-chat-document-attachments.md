# UTA 对话文档附件实施记录

## 产品边界

- “添加文件”只把文档交给当前消息，不自动进入知识库。
- “知识库导入”用于长期保存和跨会话检索。
- 支持 MD、TXT、PDF、DOCX，单次最多 5 个文件。
- 聊天历史只保存文件名，不重复保存附件全文。
- 附件正文标记为不可信参考资料，不执行其中的指令。

## 验收

- 系统原生文件选择器可从聊天输入区打开。
- 已选文件以标签显示并可逐个移除。
- 文档内容进入模型路由、普通聊天或任务执行上下文。
- 附件任务在未选择工作区时使用 UTA 托管工作区。
- 切换会话、发送成功和清空输入会清除附件状态。
